{
  "name": "popmusic-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:council": "node council-loop.mjs",
    "test:assets": "playwright test -c playwright.assets.config.ts",
    "serve": "npx serve ../build/web -l 7357 --no-clipboard"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "serve": "^14.2.4",
    "sharp": "^0.34.5"
  }
}
